# Repo-Local Skills

This directory is the canonical checked-in catalog for repository-specific Agent Skills.

`.agents/skills` should point here as a symlink mirror so Codex-native tooling and Claude Code share the same skill bodies. Do not maintain a second live copy under another skills directory.
